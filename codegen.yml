generates:
  ./apis/gql/schema-first-style/schema/type.ts:
    schema: './apis/gql/schema-first-style/schema/index.graphql'
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: true
